@model SonmezERP.Models.ProductLog

@{
    ViewData["Title"] = "StockIncrease";
}
<h1 class="text-center">Stok Girişi</h1>

<div class="d-flex justify-content-evenly">
    <form asp-action="StockIncrease">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="row">
            <div class="form-group col-auto">
                <label asp-for="Product.Id" class="control-label"></label>
                <select asp-for="Product.Id" class="form-control">
                   @*  <option>Loft1</option>
                    @foreach (var item in ViewBag.Products)
                    {
                        <option value="@item.ProductNameTr">@item.ProductNameTr - @item.Product.Color.ColorName</option>
                    } *@
                </select>
            </div>
            <div class="form-Control">
               <label asp-for="Input" class="control-label"></label>
               <input asp-for="Input" class="form-control"/>
               <span asp-validation-for="Input" class="text-danger"></span>
            </div>




            
        </div>
    </form>
</div>